name: CI

# Build + test on every push to main and every PR.
# Public-repo macOS runner minutes are free, so we run on every event
# without trying to cache. The full suite (~846 tests in 117 suites)
# completes in under a second once compiled; total wall-clock per run
# is dominated by runner startup + Swift package resolve.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# Cancel in-progress CI runs for a branch when a newer commit comes in
# on the same branch — saves runner minutes when force-pushing or
# pushing fixups to an open PR.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-test:
    name: swift build + swift test
    # macos-15 = Apple Silicon w/ Xcode 16 (Swift 6.x). macos-14 ships
    # Swift 5.9 which can't compile swift-argument-parser 1.7.0+
    # (uses access-level-on-imports, a Swift 6 feature). The produced
    # app's deployment target stays macOS 14 — that's controlled by
    # Package.swift's `.macOS(.v14)`, not by the build host.
    runs-on: macos-15

    steps:
      - name: Checkout
        # Pinned by full commit SHA per code-standards.md. Update by
        # bumping both the SHA and the trailing comment when a new
        # release ships.
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Show toolchain
        run: |
          swift --version
          xcodebuild -version

      - name: Build
        working-directory: App
        run: swift build

      - name: Test (single retry on flake — see issue #67)
        working-directory: App
        # Default parallel test scheduler has occasionally tripped a
        # transient `Fatal error: Range requires lowerBound <=
        # upperBound` inside Swift's own arm64e module interface — a
        # toolchain quirk, not a real test failure. One retry catches
        # that without masking real regressions: both attempts must
        # fail for the step to fail. The `::warning::` annotation
        # makes retries visible in the PR summary so we can escalate
        # to `--num-workers 1` if the flake becomes recurrent.
        run: |
          if ! swift test; then
            echo "::warning::First swift test attempt failed — retrying once. If this annotation appears repeatedly, escalate per issue #67 (consider --num-workers 1)."
            swift test
          fi

  build-test-linux:
    name: swift build + swift test (Linux)
    # A REQUIRED CHECK. Linux is expected to build and pass, and a red
    # check here is a real failure, not an informational one. This job
    # carried failure tolerance while the Linux port was known-broken;
    # Linux now compiles and passes, so the tolerance is gone and the
    # check reports on its own merits.
    #
    # Test-count reconciliation: Linux runs 854 of the macOS job's 856
    # tests. The difference is exactly two `@Test` declarations that are
    # compiled out off-Apple — nothing is silently skipped:
    #
    #   Tests/SymbolLogTests.swift          `#if canImport(os)`
    #     "default backend is the os.Logger backend on Apple platforms"
    #   Tests/SymbolServerTrustTests.swift  `#if canImport(Security)`
    #     pinCertificateSessionHasDelegate()
    #
    # 856 - 2 = 854. If the two totals ever drift further apart, the gap
    # is new Darwin-only coverage (or lost Linux coverage) and needs
    # explaining here — do not just update the numbers.
    runs-on: ubuntu-latest
    # The official Swift image rather than a marketplace setup action:
    # GitHub's Ubuntu runners ship no Swift toolchain, and an action
    # would add a third-party supply-chain dependency to pin and track.
    # Noble is the current Ubuntu LTS. Swift 6.1 matches the macOS job's
    # toolchain series so a failure here is attributable to the platform
    # rather than to a compiler-version difference.
    #
    # Pinned by DIGEST. This comment used to say "pinned tag ... for the
    # same reason the checkout action is SHA-pinned", which did not
    # follow: `6.1-noble` is mutable and a commit SHA is not, so the tag
    # gave none of the immutability the comparison claimed. This is the
    # multi-arch index digest for swift:6.1-noble; bump it deliberately
    # when moving Swift versions, together with release.yml's copy.
    container: swift@sha256:fddaf02db3d41844916167ef4d199d5ca14c6003d052a0b9ab579646a9c720ec

    steps:
      - name: Checkout
        # Same pin as the macOS job — bump both together.
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Show toolchain
        # No xcodebuild off-Apple. This line is the record of which Swift
        # the image actually shipped, which is what the next round of
        # Linux work targets.
        run: swift --version

      - name: Build
        working-directory: App
        # Package.swift drops the SwiftUI app target on platforms without
        # SwiftUI, so this builds the core library, the CLI, and the
        # tests only. If it tries to build the GUI target here, that is a
        # real finding about the manifest — report it rather than adding
        # a target filter.
        #
        # Compiler output is printed in full — never redirected,
        # quietened, or truncated.
        run: swift build

      - name: Test
        working-directory: App
        # No retry wrapper: the macOS retry exists for a specific arm64e
        # toolchain flake that does not apply here. A Linux failure is a
        # real regression, not something to retry past.
        #
        # A failed Build skips this step, which is intended: the test
        # target depends on the library, so a Core compile error would
        # only resurface here as a confusing test-discovery failure.
        run: swift test

      - name: CLI smoke test
        working-directory: App
        # Build and Test above prove the code compiles and the library
        # behaves. Neither one ever runs the shipped binary, which is the
        # only thing a Linux user installs. This step runs it, and fails
        # the job when it misbehaves: a Linux CLI regression should block
        # merge for the same reason a failing test does.
        #
        # What it asserts is deliberate. `info` must report the fixture's
        # stream count, and `analyze` must exit 2 — the documented
        # crash-detected code for a dump containing an access violation.
        # Asserting a bare exit 0 instead would pass on a binary that
        # started up and parsed nothing. The stream count is a property of
        # test.dmp rather than of output formatting, so it does not move
        # when the report layout changes; the timestamp line, by contrast,
        # renders per-locale and would be a brittle marker.
        #
        # Exit statuses are captured directly from the binary, never read
        # through a pipe. `minidumptruck-cli … | grep` reports grep's
        # status, not the binary's, and that mistake has produced false
        # passes in this repo before. Output is written to a file first,
        # and the file is what gets searched.
        #
        # TestData/test.dmp is tracked (.gitignore ignores *.dmp, then
        # re-includes App/TestData/*.dmp), so it is already in the
        # checkout and needs no download.
        run: |
          FIXTURE=TestData/test.dmp
          CLI=.build/release/minidumptruck-cli
          OUT=$(mktemp -d)
          FAILED=0

          # Release, not debug: the release binary is what ships, and a
          # failure that only appears under optimisation would otherwise
          # go unnoticed until a user hit it.
          swift build -c release --product minidumptruck-cli

          if "$CLI" info "$FIXTURE" > "$OUT/info.txt" 2>&1; then
            INFO_RC=0
          else
            INFO_RC=$?
          fi
          cat "$OUT/info.txt"

          if [ "$INFO_RC" -ne 0 ]; then
            echo "FAIL: 'info' exited $INFO_RC, expected 0."
            FAILED=1
          fi

          if grep -q 'Streams: 9' "$OUT/info.txt"; then
            echo "OK: 'info' reported the expected stream count."
          else
            echo "FAIL: 'info' did not report 'Streams: 9' for test.dmp — the binary ran but did not parse the fixture."
            FAILED=1
          fi

          if "$CLI" analyze "$FIXTURE" > "$OUT/analyze.txt" 2>&1; then
            ANALYZE_RC=0
          else
            ANALYZE_RC=$?
          fi
          cat "$OUT/analyze.txt"

          if [ "$ANALYZE_RC" -eq 2 ]; then
            echo "OK: 'analyze' exited 2, the documented crash-detected code."
          else
            echo "FAIL: 'analyze' exited $ANALYZE_RC, expected 2 (crash detected) for test.dmp."
            FAILED=1
          fi

          if [ "$FAILED" -ne 0 ]; then
            echo "Linux CLI smoke test failed — the shipped binary does not work on Linux."
            exit 1
          fi
